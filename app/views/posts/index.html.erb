<h1>Top Page</h1>
<%= link_to '新規投稿', '/posts/new' %>
<% @post.each do |post| %>
  <div class="post">
    <div class="post-content">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-date">
      <%= post.content %>
    </div>
  </div>
<% end %>